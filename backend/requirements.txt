# Pinned-floor runtime deps for the email-verification engine and its host app.
# Mirrors [project.dependencies] in pyproject.toml for plain `pip install -r`.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
dnspython>=2.6
aiosmtplib>=3.0
python-socks>=2.4
langgraph>=0.2,<0.3
langchain-google-genai>=2.0,<3.0
# Sourcing (Google Jobs via SerpAPI) + background scheduler
httpx>=0.27               # async HTTP client for SerpAPI calls
apscheduler>=3.10,<4      # AsyncIOScheduler for the periodic sourcing job
